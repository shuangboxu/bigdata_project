# Copy this file to ".env" and fill in the values you need.
# Lines beginning with "#" are comments and will be ignored.

# Path to the input Excel/CSV used by the agent workflow.
DATA_FILE=data/数据示例.xlsx

# Where to write the generated HTML summary.
WORKFLOW_HTML=reports/workflow_overview.html

# Optional: OpenAI API key for the reporter agent (leave blank to use the template fallback).
OPENAI_API_KEY=
